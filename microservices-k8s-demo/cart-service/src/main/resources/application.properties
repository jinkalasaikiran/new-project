spring.application.name=cart-service
server.port=5003
management.endpoints.web.exposure.include=health,info

# URL of the product-service. In Kubernetes this resolves via the Service DNS name.
# Overridable via the PRODUCT_SERVICE_URL environment variable.
product.service.url=${PRODUCT_SERVICE_URL:http://localhost:5002}
